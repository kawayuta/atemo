<%= form_with(model: emo, local: true) do |form| %>
  <% if emo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emo.errors.count, "error") %> prohibited this emo from being saved:</h2>

      <ul>
      <% emo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_area :text, id: :emo_text, placeholder:'「ひとりごと」や「言葉」を自由に入力してください。' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
